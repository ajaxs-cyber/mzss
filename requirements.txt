# Web 框架
Django>=4.2,<5.0

# WSGI 服务器
gunicorn>=21.0

# 数据库适配（PostgreSQL 用，SQLite 不需要额外包）
dj-database-url>=2.0
psycopg2-binary>=2.9

# 静态文件（WhiteNoise，生产环境 serve 静态文件用）
whitenoise>=6.6

# 七牛云对象存储（如果你用了）
qiniu>=7.10

# 图片处理（如果你上传了图片/头像）
Pillow>=10.0

# HTTP 请求
requests>=2.31

# 环境变量管理
python-dotenv>=1.0
