# ═══════════════════════════════════════════
# 觅知音 - 音乐场景量化匹配系统 依赖列表
# ═══════════════════════════════════════════

# ── 核心框架 ──
Django>=4.2,<5.0                # Django Web 框架（LTS 版本）
djangorestframework>=3.14       # Django REST Framework - API 开发
django-filter>=23.0             # 高级过滤器支持
django-cors-headers>=4.0        # CORS 跨域支持
whitenoise>=6.5                 # 静态文件服务（生产环境）

# ── 数据库 ──
# SQLite（Django 内置，无需额外依赖）
# PostgreSQL（生产环境推荐）
psycopg2-binary>=2.9           # PostgreSQL 适配器（可选）
dj-database-url>=2.0           # DATABASE_URL 环境变量解析（可选）

# ── 部署与运维 ──
gunicorn>=21.0                 # WSGI HTTP 服务器（生产环境）

# ── 开发工具（可选）──
# django-debug-toolbar>=4.0     # 调试工具栏
# ipython>=8.0                  # 增强型交互式 shell
